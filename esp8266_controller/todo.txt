ideas:
- command line interpreter, der die modi wechseln kann oder zB initial position annehmen kann
- Log Klasse, die verschiede level loggen und ausgeben kann
- strings aus webserver kann man konstant machen, string dauert doch immer zeit...
- Anchor klasse verstehen
- evtl. kann man das movement noch weiter in die anchor verschieben?
- interface um initiale position einzustellen
- Config in EEPROM https://github.com/esp8266/Arduino/tree/master/libraries/EEPROM
- position beim spoolen updaten (wie geht das? Dazu muss ich wissen, wie man die position invers berechnen)
- actual position zuzsätzlich direct in die felder schreiben
- WiFiConnection von außen IP ändern.
- Connection in ConnectionMgr packen und das dahin auslagern. Singleton, der nur einmal den serial callback anlegt.
- command, der alle registrierten commands ausgibt
